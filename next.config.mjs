/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'standalone'
};

module.exports = {
    assetPrefix: './',
    basePath: '/Receita-Next-app',
    trailingSlash: true,
}

export default nextConfig;
